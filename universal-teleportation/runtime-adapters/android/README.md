# runtime-adapters/android

## Purpose
Contains Android-specific runtime adaptation logic used during capture and reconstruction.

## Key Modules
- `android_adapter.py`: Android runtime abstraction and integration points.
