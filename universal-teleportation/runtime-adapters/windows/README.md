# runtime-adapters/windows

## Purpose
Provides Windows runtime and filesystem adaptation layers for migration compatibility.

## Key Modules
- `windows_adapter.py`: Windows runtime integration.
- `windows_fs_mapper.py`: Windows filesystem mapping helpers.
